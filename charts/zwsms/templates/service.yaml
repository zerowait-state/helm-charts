apiVersion: v1
kind: Service
metadata:
  name: {{ include "zwsms.fullname" . }}
  labels:
    {{- include "zwsms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zwsms.selectorLabels" . | nindent 4 }}
